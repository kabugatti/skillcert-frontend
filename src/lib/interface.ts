import { JSX } from "react";

export interface WelcomePageBlockProps {
  icon: JSX.Element;
  text: string;
  figure: number;
}

export interface featuredCourseProps {
  id: number;
  title: string;
  description: string;
  img: string;
}
